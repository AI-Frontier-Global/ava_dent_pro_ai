# دليل النشر الحي — DentalPro SaaS

دليل بسيط جداً لنشر المنصة والحصول على رابط حي ترسله للعيادات.

---

## الخطوة 1: إنشاء قاعدة البيانات (Supabase)

المشروع لديك على Supabase جاهز مسبقاً. كل ما تحتاجه هو نسخ المفاتيح.

1. اذهب إلى [supabase.com](https://supabase.com) وسجّل الدخول.
2. افتح مشروعك (الموجود مسبقاً).
3. من القائمة الجانبية، اضغط **Settings** ثم **API**.
4. انسخ هذه القيم الثلاث:
   - **Project URL** (مثل: `https://xxxxx.supabase.co`)
   - **anon public key** (مفتاح عام طويل)
   - **service_role key** (مفتاح سري — لا تشاركه مع أحد)

> الجداول (clinics, subscriptions, clinic_members, support_tickets, clinic_plans) تم إنشاؤها بالفعل في قاعدة البيانات. لا تحتاج لفعل أي شيء إضافي.

---

## الخطوة 2: الحصول على مفتاح OpenAI (اختياري — للشات بوت الذكي)

1. اذهب إلى [platform.openai.com](https://platform.openai.com).
2. سجّل الدخول أو أنشئ حساب.
3. اضغط **API Keys** ثم **Create new secret key**.
4. انسخ المفتاح (يبدأ بـ `sk-`).

> هذا المفتاح اختياري. الشات بوت يعمل بردود ذكية مبنية على الكلمات المفتاحية بدون أي مفتاح. لكن مع مفتاح OpenAI، ستكون الردود أذكى وأكثر طبيعية.

---

## الخطوة 3: رفع النظام على Vercel (مجاني)

### الطريقة الأسهل: عبر GitHub

1. اذهب إلى [github.com](https://github.com) وأنشئ مستودعاً (repository) جديداً باسم `dentalpro-saas`.
2. ارفع ملفات المشروع إلى المستودع:
   ```
   git init
   git add .
   git commit -m "DentalPro SaaS"
   git branch -M main
   git remote add origin https://github.com/USERNAME/dentalpro-saas.git
   git push -u origin main
   ```
   (استبدل `USERNAME` باسم حسابك في GitHub)

3. اذهب إلى [vercel.com](https://vercel.com) وسجّل الدخول بحساب GitHub.
4. اضغط **Add New Project**.
5. اختر مستودع `dentalpro-saas`.
6. في صفحة الإعدادات:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
7. اضغط **Environment Variables** وأضف هذه المتغيرات:

   | Name | Value |
   |------|-------|
   | `VITE_SUPABASE_URL` | (الرابط من الخطوة 1) |
   | `VITE_SUPABASE_ANON_KEY` | (المفتاح العام من الخطوة 1) |

8. اضغط **Deploy**.

### انتظر 2-3 دقائق...

سيتم بناء المشروع ونشره. ستحصل على رابط حي مثل:

```
https://dentalpro-saas.vercel.app
```

هذا هو الرابط الذي ترسله للعيادات!

---

## الخطوة 4: إضافة مفتاح OpenAI داخل النظام (اختياري)

بعد فتح الرابط الحي في المتصفح:

1. سجّل الدخول كمسؤول عيادة.
2. اذهب إلى **مركز الذكاء الاصطناعي**.
3. في تبويب **مفاتيح API**، اختر OpenAI.
4. الصق مفتاح OpenAI وفعّل الموفر.
5. اضغط **حفظ**.

الآن الشات بوت في مركز المساعدة سيستخدم OpenAI للرد بذكاء على الأسئلة.

---

## الخطوة 5: إرسال الرابط للعيادات

الآن لديك رابط حي. أرسل للعيادات رسالة مثل:

> "جرّب نظام إدارة عيادة الأسنان مجاناً لمدة 14 يوماً:
> https://dentalpro-saas.vercel.app
> 
> اضغط "تجربة مجانية" — لا تحتاج بطاقة ائتمان."

---

## ملاحظات مهمة

- **النشر مجاني**: Vercel يوفر خطة مجانية كافية لمرحلة الاختبار.
- **القاعدة البيانات مجانية**: Supabase يوفر خطة مجانية (500MB).
- **التجربة المجانية 14 يوماً**: تعمل تلقائياً عند التسجيل — بدون أي إعداد إضافي.
- **الدفع معطّل حالياً**: لا يُطلب Stripe أو بطاقة ائتمان. سنفعّله عند الإطلاق الرسمي.
- **النسخ الاحتياطي**: Supabase يعمل نسخاً احتياطية يومية تلقائياً في الخطة المدفوعة. في الخطة المجانية، يمكنك تصدير البيانات يدوياً من لوحة التحكم.

---

## حل المشاكل الشائعة

### المشكلة: الصفحة بيضاء بعد فتح الرابط
**الحل**: تأكد أنك أضفت `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` في إعدادات Vercel (Environment Variables).

### المشكلة: التسجيل لا يعمل
**الحل**: تأكد أن مفاتيح Supabase صحيحة. اذهب إلى Supabase > Settings > API وتأكد من نسخ القيم بالكامل.

### المشكلة: الشات بوت يرد بردود بسيطة
**الحل**: هذا طبيعي. لإرداد ذكية بالذكاء الاصطناعي، أضف مفتاح OpenAI من الخطوة 4.
